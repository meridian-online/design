# Allowlist for scripts/check-public-hygiene.sh.
#
# Empty on purpose, and it should stay that way. If the gate fires, the fix is
# to delete the private planning identifier — not to record it here.
#
# Format, one entry per line:
#
#     path/to/file:<exact matched text>  # why this one is legitimate
#
# The trailing `#` explanation is REQUIRED; an entry without one fails the check
# with exit 2, so nothing can be waved through silently. Line numbers are not
# part of an entry — they drift, and a stale allowlist entry that quietly stops
# matching is worse than no allowlist at all.
#
# Before adding anything here, try to make the pattern in
# scripts/check-public-hygiene.sh more precise instead. The allowlist is for
# genuine content that must stay, never for papering over a bad regex.
